
// to persist data in session and cookies to handle user requests 
// for same session

// we store user data in memory and client side to handle same user request
// if user logged in we need to sored information that user for next same user request to handle that
// Basic that we need member user all his requst that session
// that why we use session or cookies


// every request treated as a separte 
// we need to set everyTime

// also we have alternative of global variable -- but this can shared we multipal users


// cookie-parse we can use that module

// http request every client-server request forget about each other
// we need to remender our client that we need to use session
// session stored on server
// cookies are stroed on client browser

//console.log(name,password);