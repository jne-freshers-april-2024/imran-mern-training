// CSRF - Cross site request forgery

// access other pepole session 

// send money  fake sit - send in email - link leading to your page send form similare page

// session can be stilled from user

// npm install --save csurf

// token can generate alway check that token for important routes and request 
// every page genrate new token

// const csrf = require('csurf');
// const csrfProtection = csrf();
// app.use(csrfProtection);

// npm install --save connect-flash

// req.flash('error-messsage','Invalid email entered')