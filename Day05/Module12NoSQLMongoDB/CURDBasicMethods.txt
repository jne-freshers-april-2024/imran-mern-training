/*
// return promise 
     db.collection('name').insertOne({write Object inside it}).then().catch().
     

     // used mongodb compass connect to cluster and used insert data it provide GUI


     // DB.collection('user').find({}).toArray() // less data contains other wise pagination

     // updateOne({id:userid},{$set:this}) and deleteOne method to
     update and delete on product or user

     save()

*/

// Mongoodb connection

//const mongoConnect = require('./databaseConnection'); ----------------